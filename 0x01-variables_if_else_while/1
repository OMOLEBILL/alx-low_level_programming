#include <stdio.h>

/**
 * main - Entry point 
 *
 * Description: Alphabet except some 
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha;

	for (alpha = 'a'; alpha = 'z'; ++c)
	{
	if (alpha != 'e' && alpha != 'q')
	putchar(alpha);
	}
	putchar('\n');

	return (0);
}
