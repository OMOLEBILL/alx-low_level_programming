#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Integers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; ++a)
		printf("%i", a);
	putchar('\n');

	return (0);
}
