#include "main.h"
/**
 * mul - mproduct of two integers
 * @a: ASCII character
 * @b: ASCII character
 * Return: Product of two numbers
 */
int mul(int a, int b)
{
	int z;

	z = (a * b);

	return (z);
}
